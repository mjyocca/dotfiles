# ~/.zshenv — sourced by zsh for every shell (login, interactive, script)
# Managed by the scripts stow package so ~/.local/bin is always in PATH
# regardless of whether the zsh package is stowed on this machine.

export PATH="$HOME/.local/bin:$PATH"
